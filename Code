function fizzBuzz() {        
        for (let number = 1; number <= 100; number++) {
                let fizz = number % 3 === 0, buzz = number % 5 === 0;
                console.log (fizz ? buzz ? "FizzBuzz" : "Fizz" : buzz ? "Buzz" : number);
        }
}

fizzBuzz();
